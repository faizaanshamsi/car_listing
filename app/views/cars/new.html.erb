<h1>Create a New Car</h1>

<%= simple_form_for @car do |f| %>
 <% if @car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @car.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.input :color, label: 'Color' %>
  <%= f.input :year, label: 'Year' %>
  <%= f.input :mileage, label: 'Mileage' %>
  <%= f.input :description, label: 'Description', required: false %>
  <%= f.button :submit, 'Create New Car' %>
<% end %>
